export const ROUTES = {
	home: '/',
	profile: '/profile',
	community: '/community',
	links: '/links',
	contacts: '/contacts',
	all: '*',
};

export const {home, profile, community, contacts, links, all} = ROUTES;
